/**
 * Frozen contract: Layer 1 → Layer 2.
 * L2 accepts only APPROVED / PARTIALLY_APPROVED batches.
 * Parsing & schema detection stay in L1; L2 never re-parses raw bytes.
 */
export {};
//# sourceMappingURL=ingestion-result.js.map