# Layer 5 — Explanation

**V1 status: implemented** as **template + citations** (not an LLM calculator, not a road-cost AI).

- Inputs: Layer 3 origin decision evidence and/or Layer 4 lane decision evidence  
- Output: titled summary + labelled bullets with factIds / URLs when present  
- Rule: never recomputes demurrage ₹; never invents live AIS; never quotes inland trucking  

LLM narration remains a later option behind the same interface.
