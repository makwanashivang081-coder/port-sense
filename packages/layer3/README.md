# Layer 3 — Intelligence (Cost + Risk)

**V1 scope:** Demurrage Cost + Risk v1 only.  
Prediction ML and Transit learning are **out of V1 product** (research baseline may still be cited).

```bash
cd layer2 && npm run seed
cd ../layer3
npm install
npm run validate
npm run validate:math
npm run demo
```

Reads **only** via `@port-sense/layer2-canonical`. Never writes canonical facts.
